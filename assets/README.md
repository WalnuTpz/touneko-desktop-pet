# 素材目录

```text
assets/
├─ README.md
├─ local/
│  ├─ 糖猫合集/
│  └─ 日常与悬停/
└─ generated/
```

- `local/` 保存本地原始素材。程序和处理脚本只读取这里的文件，不修改其内容。
- `generated/` 保存尺寸归一化、重命名等流程生成的素材副本。
- `local/` 和 `generated/` 都被 Git 忽略，不会随源码提交或上传。
- 新增素材时，先放入对应的 `local/` 子目录，再运行素材检查或后续处理脚本。
